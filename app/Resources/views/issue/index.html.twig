{% extends 'base.html.twig' %}

{% block body %}

    {% for document in documents %}
        {{ loop.index }}<br>
        {{ document.id }}<br>
        {{ document.category.name }}<br>
        {{ document.file.filename }}<br>
        {{ document.mimeType }}<br>

    {% endfor %}
{% endblock %}

