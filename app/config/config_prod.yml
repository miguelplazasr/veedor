imports:
    - { resource: config.yml }

#framework:
#    validation:
#        cache: validator.mapping.cache.doctrine.apc
#    serializer:
#        cache: serializer.mapping.cache.doctrine.apc

#doctrine:
#    orm:
#        metadata_cache_driver: apc
#        result_cache_driver: apc
#        query_cache_driver: apc

monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: error
            handler:      nested
        nested:
            type:  stream
            #path:  "%kernel.logs_dir%/%kernel.environment%.log" #Se comentó esta linea para hacer despliegue de la aplicación en Heroku
            path: 'php://stderr' # Esta linea es necesario para manejar los logs en Heroku
            level: debug
        console:
            type:  console
